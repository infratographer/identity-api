apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.names.fullname" . }}
spec:
  ports:
    - name: web
      port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: {{ template "common.names.name" . }}
    component: app
  sessionAffinity: None
  type: ClusterIP
